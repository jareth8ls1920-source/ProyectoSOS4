extends ../layout

block content
  .card.card-form.text-center
    h1 📧 Email enviado
    p Te enviamos un email con un enlace para restablecer tu password.
    p.muted Revisá tu bandeja de entrada (y la carpeta de spam). El enlace expira en 1 hora.
    a.btn.btn-primary(href='/login') Volver al login
